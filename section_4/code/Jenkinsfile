#!/usr/bin/groovy

node {
    		envList = "Oracle12c\nOracle19c"
	choiceArray = [ "patch" , "minor" , "major" ]
	     }
pipeline {
	agent any
	
/*
	 parameters {
        	choice(choices: "${envList}", name: 'OracleVersion', description: 'please choose the Oracle version you want to deploy?')
   
   	 }
*/	
	


	parameters{
            choice(choices: choiceArray.collectEntries { "$it\n" }.join(' ') ,
                    description: '',
                    name: 'OracleVersion')

	}

	
	stages {

		stage('Build') {
			steps {
				echo "Building..${params.OracleVersion}"
			}
		}

		stage('Test') {
			steps {
				echo 'Testing..'
			}
		}

		stage('Deploy') {
			steps {
				echo 'Deploying....'
			}
		}

	}
}
